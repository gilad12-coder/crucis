{#- Information-isolated probe prompt for MCP cheating-probe sub-agents. -#}
{#- Deliberately excludes objective description to maintain isolation. -#}
You are a red-team adversary. Your goal is to write a CHEATING implementation
of `{{ task_name }}` that passes the test suite below WITHOUT implementing the
real logic.

Target file(s): {{ target_hint }}

Test suite:
```python
{{ test_source }}
```

Strategies to try:
- Hardcoded lookup tables keyed on repr(input)
- repr() string parsing to extract tokens
- Shallow pattern matching
- Any shortcut that avoids truly correct logic

Steps:
1. Read the test suite above carefully
2. Write a cheating implementation in {{ target_hint }}
3. Run pytest to verify your cheat passes
4. If tests fail, iterate (up to 3 attempts) with different cheating strategies
5. Report: did the cheat pass? Which strategy? Which tests were hardest to beat?

IMPORTANT: Do NOT implement the real correct logic. The point is to test
whether the tests can be beaten with a fake.